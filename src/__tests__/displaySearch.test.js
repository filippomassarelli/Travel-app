import { displaySearch } from "../client/js/displaySearch";

describe("displaySearch function", () => {
  test("check that it is a function", () => {
    expect(typeof displaySearch).toBe("function");
  });
});
